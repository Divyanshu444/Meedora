export const IMAGE = `fragment ImageFields on Image { url altText width height }`
export const VARIANT = `fragment VariantFields on ProductVariant { id title availableForSale price { amount currencyCode } compareAtPrice { amount currencyCode } selectedOptions { name value } image { ...ImageFields } }`
export const PRODUCT = `fragment ProductFields on Product { id handle title description productType tags availableForSale updatedAt featuredImage { ...ImageFields } images(first: 12) { nodes { ...ImageFields } } priceRange { minVariantPrice { amount currencyCode } maxVariantPrice { amount currencyCode } } variants(first: 100) { nodes { ...VariantFields } } options { name values } seo { title description } metafields(identifiers: [{namespace: "custom", key: "material"}, {namespace: "custom", key: "plating"}, {namespace: "custom", key: "care"}, {namespace: "custom", key: "styling"}, {namespace: "custom", key: "size"}]) { key value } } ${VARIANT} ${IMAGE}`
export const COLLECTION = `fragment CollectionFields on Collection { id handle title description image { ...ImageFields } seo { title description } }`
export const PRODUCTS_QUERY = `query Products($first: Int!, $after: String, $query: String, $sortKey: ProductSortKeys, $reverse: Boolean) { products(first: $first, after: $after, query: $query, sortKey: $sortKey, reverse: $reverse) { nodes { ...ProductFields } pageInfo { hasNextPage endCursor } } } ${PRODUCT}`
export const PRODUCT_QUERY = `query Product($handle: String!) { product(handle: $handle) { ...ProductFields } } ${PRODUCT}`
export const COLLECTIONS_QUERY = `query Collections { collections(first: 50) { nodes { ...CollectionFields } } } ${COLLECTION} ${IMAGE}`
export const COLLECTION_QUERY = `query Collection($handle: String!, $first: Int!, $after: String, $sortKey: ProductCollectionSortKeys, $reverse: Boolean) { collection(handle: $handle) { ...CollectionFields products(first: $first, after: $after, sortKey: $sortKey, reverse: $reverse) { nodes { ...ProductFields } pageInfo { hasNextPage endCursor } } } } ${COLLECTION} ${PRODUCT}`
export const RECOMMENDATIONS_QUERY = `query Related($productId: ID!) { productRecommendations(productId: $productId) { ...ProductFields } } ${PRODUCT}`
export const SHOP_QUERY = `query Shop { shop { name description primaryDomain { url } privacyPolicy { title body url } refundPolicy { title body url } shippingPolicy { title body url } termsOfService { title body url } } }`
export const SEARCH_QUERY = `query Search($query: String!, $first: Int!, $after: String) { search(query: $query, first: $first, after: $after, types: [PRODUCT], unavailableProducts: LAST) { nodes { ... on Product { ...ProductFields } } pageInfo { hasNextPage endCursor } } } ${PRODUCT}`
export const CART = `fragment CartFields on Cart { id checkoutUrl totalQuantity cost { subtotalAmount { amount currencyCode } totalAmount { amount currencyCode } } lines(first: 100) { nodes { id quantity cost { totalAmount { amount currencyCode } } merchandise { ... on ProductVariant { ...VariantFields product { id title handle } } } } } } ${VARIANT} ${IMAGE}`
export const CART_QUERY = `query Cart($id: ID!) { cart(id: $id) { ...CartFields } } ${CART}`
export const CART_CREATE = `mutation CreateCart($input: CartInput!) { cartCreate(input: $input) { cart { ...CartFields } userErrors { field message } warnings { message } } } ${CART}`
export const CART_ADD = `mutation AddLines($cartId: ID!, $lines: [CartLineInput!]!) { cartLinesAdd(cartId: $cartId, lines: $lines) { cart { ...CartFields } userErrors { field message } warnings { message } } } ${CART}`
export const CART_UPDATE = `mutation UpdateLines($cartId: ID!, $lines: [CartLineUpdateInput!]!) { cartLinesUpdate(cartId: $cartId, lines: $lines) { cart { ...CartFields } userErrors { field message } warnings { message } } } ${CART}`
export const CART_REMOVE = `mutation RemoveLines($cartId: ID!, $lineIds: [ID!]!) { cartLinesRemove(cartId: $cartId, lineIds: $lineIds) { cart { ...CartFields } userErrors { field message } warnings { message } } } ${CART}`
export const VARIANT_QUERY = `query Variant($id: ID!) { node(id: $id) { ... on ProductVariant { id availableForSale quantityAvailable price { amount currencyCode } } } }`
